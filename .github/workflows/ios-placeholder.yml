name: iOS CI (placeholder)

# Fires on pull_request when iOS files change.
# Jobs are no-ops that satisfy the Lint / Test / Migrations status-check
# contract on iOS-only PRs. Replace with real Xcode CI in #64.

on:
  pull_request:
    paths:
      - "ios/**"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: "Placeholder — replaced by real iOS CI in #64"
        run: echo "iOS lint placeholder — no Swift linting configured yet."

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: "Placeholder — replaced by real iOS CI in #64"
        run: echo "iOS test placeholder — no XCTest runner configured yet."

  migrations:
    name: Migrations
    runs-on: ubuntu-latest
    steps:
      - name: "Placeholder — replaced by real iOS CI in #64"
        run: echo "iOS migrations placeholder — iOS client has no migrations."
