import SwiftUI
import OracleCore

// MARK: - MemoryDetailView (stub — see #207 green commit for full implementation)
//
// Placeholder so the app target compiles during the TDD red commit.
// The full view is implemented in the green commit.

struct MemoryDetailView: View {
  let result: QueryResult
  let onDeleteSuccess: (UUID) -> Void

  var body: some View {
    // TODO(#207): implement full detail view
    Text("Memory Detail")
  }
}
