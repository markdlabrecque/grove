import SwiftUI

/// Tasks tab — lists all incomplete EKReminders from the device.
///
/// TODO(#438): implement full state machine, permission flow, and list rendering.
struct TasksView: View {
  var body: some View {
    Text("Tasks")
  }
}

#Preview {
  TasksView()
}
